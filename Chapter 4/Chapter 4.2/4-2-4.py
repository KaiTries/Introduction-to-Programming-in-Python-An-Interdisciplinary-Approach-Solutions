#the mechanism depends on the ascending order otherwise it will not work and jump in the wrong directions
#it relys on the fact that if a given mid is smaller than x all indexes left of mid are also smaller

